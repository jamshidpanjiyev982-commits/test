# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://jamshidpanjiyev982_db_user:J321478569p.@test.bwyepuo.mongodb.net/quizverse?retryWrites=true&w=majority

# JWT signing secret (generate with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))")
JWT_SECRET=replace-with-a-long-random-string

# Token lifetime (default: 12h). Examples: 1h, 7d, 12h
JWT_EXPIRES_IN=12h

# Local dev port (Vercel/Netlify ignore this)
PORT=3000

# Allowed CORS origin (use "*" for dev, your domain for prod)
CORS_ORIGIN=*
